version: "3.9"

services:
  backend:
    build: .
    container_name: registration_backend
    ports:
      - "8500:8500"                               # Backend API port
    env_file:
      - ./.env
    volumes:
      - image_data:/app/public/uploads                 # Persist uploaded files
    restart: always

volumes:
  image_data:
